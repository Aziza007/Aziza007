function click(401,953){
    var ev = document.createEvent("MouseEvent");
    var el = document.elementFromPoint(401,953);
    ev.initMouseEvent(
        "click",
   var result = str.link("semele.html")
   document.getElementById("demo").innerHTML = result;
    );
    el.dispatchEvent(ev);
}
